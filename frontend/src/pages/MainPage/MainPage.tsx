import Title from "@/components/ui/title";

const MainPage = () => {
  return (
    <div>
      <div className="w-full flex items-center justify-between max-sm:block">
        <Title size="md" text="Добро пожаловать" />
      </div>
    </div>
  );
};

export default MainPage;
